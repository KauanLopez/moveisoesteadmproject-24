import { StoredExternalCatalog } from '@/services/localStorageService';

// Deixando o array vazio, nenhum catálogo de exemplo será criado automaticamente.
export const defaultExternalCatalogs: StoredExternalCatalog[] = [];